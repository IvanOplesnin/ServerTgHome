services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-server_tg_home}
      POSTGRES_USER: ${POSTGRES_USER:-server_tg_home}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-server_tg_home_password}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 5s
      timeout: 5s
      retries: 12
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redis-data:/data

  go2rtc:
    image: alexxit/go2rtc:1.9.14
    restart: unless-stopped
    volumes:
      - ./config:/config:ro
      - ./data:/data:ro
    ports:
      - "127.0.0.1:1984:1984"
      - "${STH_REVERSE_PROXY_BIND_ADDRESS:-127.0.0.1}:21984:1984"
      - "8555:8555/tcp"
      - "8555:8555/udp"

  miniapp-web:
    profiles: ["miniapp"]
    build:
      context: .
      dockerfile: docker/miniapp.Dockerfile
    restart: unless-stopped
    ports:
      - "${STH_REVERSE_PROXY_BIND_ADDRESS:-127.0.0.1}:18082:8080"
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8080/healthz"]
      interval: 15s
      timeout: 5s
      retries: 5

  api:
    build: .
    restart: unless-stopped
    command:
      - "server-tg-home"
      - "api"
      - "--host"
      - "0.0.0.0"
      - "--port"
      - "8080"
      - "--no-access-log"
    environment:
      STH_CONFIG: /app/config/config.yaml
      POSTGRES_DB: ${POSTGRES_DB:-server_tg_home}
      POSTGRES_USER: ${POSTGRES_USER:-server_tg_home}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-server_tg_home_password}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      TELEGRAM_PROXY_URL: ${TELEGRAM_PROXY_URL:-}
      HOME_ASSISTANT_TOKEN: ${HOME_ASSISTANT_TOKEN:-}
      STH_WEBHOOK_TOKEN: ${STH_WEBHOOK_TOKEN:-}
    ports:
      - "127.0.0.1:18080:8080"
      - "${STH_REVERSE_PROXY_BIND_ADDRESS:-127.0.0.1}:28080:8080"
    volumes:
      - ./config:/app/config:ro
      - ./data:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  worker:
    build: .
    restart: unless-stopped
    command: ["server-tg-home", "worker"]
    environment:
      STH_CONFIG: /app/config/config.yaml
      POSTGRES_DB: ${POSTGRES_DB:-server_tg_home}
      POSTGRES_USER: ${POSTGRES_USER:-server_tg_home}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-server_tg_home_password}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      TELEGRAM_PROXY_URL: ${TELEGRAM_PROXY_URL:-}
      HOME_ASSISTANT_TOKEN: ${HOME_ASSISTANT_TOKEN:-}
    volumes:
      - ./config:/app/config:ro
      - ./data:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  graph-worker:
    build: .
    restart: unless-stopped
    command: ["server-tg-home", "graph-worker"]
    environment:
      STH_CONFIG: /app/config/config.yaml
      POSTGRES_DB: ${POSTGRES_DB:-server_tg_home}
      POSTGRES_USER: ${POSTGRES_USER:-server_tg_home}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-server_tg_home_password}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      TELEGRAM_PROXY_URL: ${TELEGRAM_PROXY_URL:-}
    volumes:
      - ./config:/app/config:ro
      - ./data:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  audio-worker:
    build: .
    restart: unless-stopped
    command: ["server-tg-home", "audio-worker"]
    environment:
      STH_CONFIG: /app/config/config.yaml
      POSTGRES_DB: ${POSTGRES_DB:-server_tg_home}
      POSTGRES_USER: ${POSTGRES_USER:-server_tg_home}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-server_tg_home_password}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      TELEGRAM_PROXY_URL: ${TELEGRAM_PROXY_URL:-}
    volumes:
      - ./config:/app/config:ro
      - ./data:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
      go2rtc:
        condition: service_started

  buffer:
    build: .
    restart: unless-stopped
    command: ["server-tg-home", "buffer"]
    environment:
      STH_CONFIG: /app/config/config.yaml
    volumes:
      - ./config:/app/config:ro
      - ./data:/data
    depends_on:
      - redis

  retention:
    build: .
    restart: unless-stopped
    command: ["server-tg-home", "retention"]
    environment:
      STH_CONFIG: /app/config/config.yaml
      POSTGRES_DB: ${POSTGRES_DB:-server_tg_home}
      POSTGRES_USER: ${POSTGRES_USER:-server_tg_home}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-server_tg_home_password}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      TELEGRAM_PROXY_URL: ${TELEGRAM_PROXY_URL:-}
    volumes:
      - ./config:/app/config:ro
      - ./data:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

volumes:
  postgres-data:
  redis-data:
